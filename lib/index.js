"use strict";

var config = require('./config'),
  sshConfig = require('./sshconfig'),
  opsWorks = require('./opsworks'),
  util = require('util');

function Yaowst(opt_options) {
  opt_options = opt_options || {};

}

